{
  "name": "swiftride",
  "private": true,
  "version": "0.2.0",
  "description": "Ride hailing where the customer picks the route and pays per kilometre. Web + installable PWA, Node API, SQLite.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "seed": "npm --prefix server run seed",
    "test": "npm --prefix server test",
    "build": "npm --prefix client run build",
    "start": "node scripts/start.mjs",
    "serve": "node scripts/start.mjs --build",
    "icons": "node client/scripts/make-icons.mjs",
    "tunnel": "npx --yes localtunnel --port 4000",
    "tunnel:dev": "npx --yes localtunnel --port 5173",
    "docker:up": "docker compose up --build -d",
    "docker:logs": "docker compose logs -f",
    "docker:down": "docker compose down",
    "android:build": "npm --prefix client run build:android && npm --prefix client exec cap sync android && cd client/android && gradlew assembleRelease",
    "android:open": "npm --prefix client exec cap open android",
    "push:keys": "npm --prefix server run push:keys",
    "start:server": "npm --prefix server start",
    "stop": "node scripts/stop.mjs",
    "autostart": "node scripts/autostart.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
